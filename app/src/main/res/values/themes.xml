<resources>
    <!-- Single source of truth for the JARVIS app theme. -->
    <style name="Theme.Jarvis" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">#061426</item>
        <item name="android:navigationBarColor">#061426</item>
        <item name="android:windowActionModeOverlay">true</item>
    </style>
</resources>
